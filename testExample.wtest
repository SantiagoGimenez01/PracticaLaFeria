import example.*

describe "Tests" {

  test "Julieta obtiene 2 tickets en el juego Tiro al blanco" {
    julieta.jugar(tiroAlBlanco)
    assert.equals(17, julieta.tickets())
  }

  test "Julieta se cansa 3 unidades en el juego de Tiro al blanco"{
    julieta.jugar(tiroAlBlanco)
    assert.equals(3, julieta.cansancio())
  }

}